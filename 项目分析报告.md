# 《天天酷跑》RAG知识库问答系统 — 项目分析报告

---

## 一、项目概述

本项目基于 **RAG（Retrieval-Augmented Generation，检索增强生成）** 架构，构建了一个面向手游《天天酷跑》的智能问答系统。系统完全运行在本地，不依赖任何云服务 API，用户可以用自然语言提问游戏相关问题，系统从本地知识库中检索相关信息，再由本地语言模型生成回答。

**核心技术栈：**

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 开发环境 | Jupyter Notebook | 交互式开发调试 |
| 编程语言 | Python 3.9 | - |
| 深度学习框架 | PyTorch | 模型推理后端 |
| LLM 大模型 | Qwen2-0.5B-Instruct | 阿里通义千问，本地生成回答 |
| Embedding 模型 | BAAI/bge-small-zh-v1.5 | 中文文本向量化 |
| 向量数据库 | FAISS | Meta 开源，高效向量检索 |
| 应用框架 | LangChain | RAG 流程编排 |

---

## 二、系统架构

### 2.1 整体架构图

```
用户提问
   │
   ▼
┌─────────────────────────┐
│  1. 文本向量化 (Embedding) │
│  bge-small-zh-v1.5      │
│  将问题转为高维向量       │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  2. 向量检索 (Retrieval)  │
│  FAISS 向量数据库         │
│  返回 Top-3 相关文本块     │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  3. 提示词拼接 (Prompt)    │
│  参考资料 + 用户问题       │
│  → 格式化为 Prompt 模板    │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  4. 回答生成 (Generation)  │
│  Qwen2-0.5B-Instruct    │
│  基于参考资料生成回答       │
└──────────┬──────────────┘
           │
           ▼
        返回回答
```

### 2.2 数据流详解

**离线阶段（知识库构建）：**

1. 读取 `data/天天酷跑知识库.txt`（173行，包含角色、坐骑、宠物、搭配、玩法5大类信息）
2. 使用 `RecursiveCharacterTextSplitter` 将文档切分为文本块，参数：`chunk_size=300`，`chunk_overlap=50`，分隔符优先级为 `\n=== ` > `\n` > 中文标点
3. 使用 `bge-small-zh-v1.5` 模型将每个文本块转为向量
4. 存入 FAISS 向量数据库，保存至 `ttkp_vector_db_local/`（index.faiss + index.pkl）
5. 第二次运行时直接加载已有向量库，跳过重复构建

**在线阶段（问答推理）：**

1. 用户输入自然语言问题
2. Embedding 模型将问题向量化
3. FAISS 检索最相似的 Top-3 文本块
4. 将检索结果与问题拼接成 Prompt
5. Qwen2-0.5B-Instruct 模型生成最终回答

---

## 三、知识库内容分析

### 3.1 知识库结构

知识库文件 `data/天天酷跑知识库.txt` 按五大板块组织，共约 173 行：

| 板块 | 内容 | 数据条目 |
|------|------|---------|
| 一、角色数据 | 10个角色（普通/稀有/史诗/传说） | 品质、技能、冷却/持续时间、加成、适配模式 |
| 二、坐骑数据 | 6个坐骑 | 品质、技能、加成、适配角色 |
| 三、宠物数据 | 4个宠物 | 类型、加成、技能、适配组合 |
| 四、最优搭配组合 | 4组推荐组合 | 总加成、特点描述 |
| 五、玩法规则 | 2个模式 | 经典模式、多人对战 |

### 3.2 分块结果

经 `RecursiveCharacterTextSplitter` 处理后，知识库被切分为 **8个文本块**。切分较小，原因是知识库文本本身以 `\n=== ` 分隔符为天然边界，文档结构化程度较高。

---

## 四、模型选型分析

### 4.1 生成模型：Qwen2-0.5B-Instruct

| 属性 | 值 |
|------|-----|
| 参数量 | 0.5B（5亿） |
| 模型文件大小 | 942 MB（safetensors 格式） |
| 推理精度 | float32 |
| 设备 | CPU |
| 生成参数 | max_new_tokens=512, temperature=0.1, top_p=0.9 |

**优势：**
- 体积小（约 1GB），适合本地部署，不依赖 GPU
- 支持中文 Instruct 指令格式，使用 `apply_chat_template` 构建 prompt

**局限性：**
- 0.5B 参数量较小，复杂推理和长文本理解能力有限
- 从实际测试看，部分回答存在幻觉（编造信息）现象，如将"齐天大圣"的技能回答为"时空之眼"（实际应为"大闹天宫"）

### 4.2 Embedding 模型：BAAI/bge-small-zh-v1.5

| 属性 | 值 |
|------|-----|
| 参数量 | 约 33M |
| 模型文件大小 | 91 MB |
| 向量维度 | 512 维 |
| 运行设备 | CPU |
| 特殊配置 | normalize_embeddings=True |

**优势：**
- 北京智源研究院出品，专为中文优化
- 轻量级，推理速度快
- 归一化后向量做余弦相似度，检索效果好

---

## 五、Prompt 设计分析

系统设计了一套结构化的 Prompt 模板，包含以下要素：

```
角色设定：你是《天天酷跑》游戏专属问答助手
约束条件：必须严格基于参考资料回答，禁止编造
输入变量：{context}（检索到的参考资料）、{question}（用户问题）
回答要求：
  1. 只回答游戏相关问题，无关问题拒绝
  2. 简洁明了，结论优先
  3. 数值必须准确
  4. 语言口语化
```

同时在 `qwen2_chat()` 函数中，system message 额外设定了角色约束。

**评价：** Prompt 设计思路清晰，通过双重约束（system message + prompt template）减少幻觉，但受限于 0.5B 模型的理解能力，实际约束效果有限。

---

## 六、代码结构分析

项目以 Jupyter Notebook 为主开发文件，按功能分为 8 个 Cell：

| Cell | 功能 | 代码行数（约） |
|------|------|--------------|
| Cell 1 | 环境配置与依赖导入 | 15 行 |
| Cell 2 | 加载 Qwen2 和 Embedding 模型 | 22 行 |
| Cell 3 | `qwen2_chat()` 推理函数 | 24 行 |
| Cell 4 | `build_ttkp_vector_db()` 向量库构建 | 27 行 |
| Cell 5 | `build_rag_chain()` RAG 问答链 | 33 行 |
| Cell 6 | `main()` 交互式主程序 | 32 行 |
| Cell 7 | `if __name__` 入口 | 2 行 |

核心函数调用链：`main()` → `build_ttkp_vector_db()` → `build_rag_chain()` → `rag_qa()` → `retriever.invoke()` + `qwen2_chat()`

**代码质量：**
- 结构清晰，函数职责单一，注释充分
- 使用异常处理保护向量库构建流程
- 交互式问答支持空输入过滤和退出指令

---

## 七、测试结果分析

从 Notebook 中记录的实际运行输出来看，系统共进行了 6 轮问答测试：

| 轮次 | 测试效果 | 评价 |
|------|---------|------|
| 第1轮 | 回复"很高兴为您解答"，未给出实质内容 | **失败** — 模型未利用检索结果 |
| 第2轮 | 回答"齐天大圣技能是时空之眼" | **失败** — 严重幻觉，正确答案应为"大闹天宫" |
| 第3轮 | 回复"没有冷却/持续的设定" | **失败** — 与知识库内容完全矛盾 |
| 第4轮 | 自我介绍，未回答具体问题 | **失败** — 模型未遵循指令 |
| 第5轮 | 正确回答"孙悟空技能是七十二变" | **成功** — 准确引用知识库 |
| 第6轮 | 部分正确但混入编造内容 | **部分成功** — 检索有效但生成不可控 |

**准确率：约 17%（1/6 完全正确，1/6 部分正确）**

---

## 八、问题诊断与改进建议

### 8.1 主要问题

#### 问题一：生成模型能力不足（核心瓶颈）

Qwen2-0.5B 参数量过小，是回答质量差的根本原因。即使检索到了正确的参考资料，模型也无法稳定地基于上下文生成准确回答。

**建议：** 升级到更大的模型，如 Qwen2-1.5B-Instruct（约 3GB）或 Qwen2-7B-Instruct（约 15GB，需 GPU）。若硬件有限，可考虑使用 **API 调用云端模型**（如智谱 GLM-4、通义千问 API）作为生成后端。

#### 问题二：知识库分块过粗

当前仅切分为 8 个文本块（整个知识库只被切了 8 块），检索粒度太粗，Top-3 返回的文本块可能包含大量无关信息，干扰模型判断。

**建议：** 调整分块参数，将 `chunk_size` 从 300 降到 100-150，使检索更精准；同时增加 `chunk_overlap` 到 30-50 保持上下文连贯。

#### 问题三：检索数量偏少

`search_kwargs={"k": 3}` 仅返回 3 个文本块，对于覆盖 10 个角色 + 6 个坐骑的知识库可能不够。

**建议：** 增加到 `k=5`，提供更丰富的上下文。

#### 问题四：缺少回答质量校验

系统没有对生成结果做后处理，无法判断回答是否真正基于参考资料。

**建议：** 可增加简单校验逻辑，例如检查回答中是否包含知识库中的关键实体词。

#### 问题五：`main.py` 未实现

项目中的 `main.py` 仍为 PyCharm 默认模板代码，未将 Notebook 中的逻辑迁移为可部署的 Python 脚本。

**建议：** 将 Notebook 代码重构为模块化的 `.py` 文件，便于部署和分发。

### 8.2 改进路线图

```
短期（快速提升）：
├── 调整分块参数（chunk_size=150, k=5）
├── 优化 Prompt 模板，增加"先引用原文再回答"的约束
└── 将 Notebook 代码整理为 main.py 脚本

中期（质量提升）：
├── 升级生成模型到 Qwen2-1.5B 或接入云端 API
├── 扩充知识库内容（更多角色、活动、攻略等）
└── 增加 Web UI 界面（Gradio / Streamlit）

长期（系统完善）：
├── 支持多轮对话，带上下文记忆
├── 增加回答引用来源标注
├── 接入用户反馈机制，持续优化
└── 考虑微调 Embedding 模型以提升领域检索精度
```

---

## 九、项目文件清单

```
天天酷跑RAG设计/
├── main.ipynb                          # 主程序（Jupyter Notebook）
├── main.py                             # 未实现（PyCharm 默认模板）
├── data/
│   ├── 天天酷跑知识库.txt                # 知识库原始数据（173行）
│   └── 转换脚本.py                      # 知识库格式转换工具
├── Qwen2-0.5B-Instruct/                # Qwen2 生成模型文件（约 1GB）
│   ├── config.json
│   ├── model.safetensors               # 模型权重
│   ├── tokenizer.json / vocab.json     # 分词器
│   └── ...
├── BAAI/models--BAAI--bge-small-zh-v1.5/  # Embedding 模型缓存（约 91MB）
│   └── snapshots/.../config.json, model.safetensors, tokenizer.json, ...
├── ttkp_vector_db_local/               # FAISS 向量数据库
│   ├── index.faiss                     # 向量索引（16KB）
│   └── index.pkl                       # 文档元数据（5.7KB）
└── ZHIPU_API_KEY.env                   # 智谱 API 密钥配置（未使用）
```

---

## 十、总结

本项目实现了一个完整的 **端到端本地 RAG 问答系统**，技术路线清晰：本地 Embedding 向量化 → FAISS 检索 → 本地 LLM 生成。系统架构合理，代码结构清晰，知识库内容完整。

主要瓶颈在于 **生成模型（Qwen2-0.5B）能力不足**，导致回答准确率偏低（约 17%）。检索环节（Embedding + FAISS）工作正常，能够返回相关文本块。

**核心改进方向：** 升级生成模型是提升系统效果的最关键一步。如果硬件允许，建议至少使用 1.5B 参数以上的模型，或者将生成环节替换为云端 API 调用，可大幅提升回答质量。
